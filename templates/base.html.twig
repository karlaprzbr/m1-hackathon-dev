<!doctype html>
<html lang="en">
  <head>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">

    <style>
    :root {
        --blanc: #FFFFFF;
        --bleu-ciel: #A7D4F5;
        --bleu-fonce: #425CB3;
        --mauve: #7E6B9E;
        --corail: #FF5E5B;
        --gris: #E5E5E5;
    }

    .bg-bleu-fonce {
        background-color: var(--bleu-fonce);
        color: var(--blanc);
        font-weight: bold;
    }

    .bg-bleu-ciel {
        background-color: var(--bleu-ciel);
        color: var(--bleu-fonce);
        font-weight: bold;
    }

    .bg-corail {
        background-color: var(--corail);
        color: var(--blanc);
        font-weight: bold;
    }

    .bg-blanc {
        background-color: var(--blanc);
    }

    .text-bleu-fonce {
        color: var(--bleu-fonce);
    }

    {# body {
        background-color: var(--gris);
    } #}

    tbody, .card {
        color: var(--bleu-fonce);
    }

    .reaction {
        width: 30px;
    }
    </style>

    <title>Maman j'ai raté l'actu</title>
  </head>
  <body>
    <nav class="navbar navbar-dark bg-bleu-fonce">
        <div class="container-fluid">
            <a class="navbar-brand" href="{{ path('podcast_index') }}">
                <img src="{{ asset('img/logo-2.jpg') }}" alt="" width="30" height="24" class="d-inline-block align-text-top">
                Maman j'ai raté l'actu
            </a>
        </div>
    </nav>

    <main class="container">
        {% block body %}{% endblock %}
    </main>

    <!-- Bootstrap Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM" crossorigin="anonymous"></script>
  </body>
</html>